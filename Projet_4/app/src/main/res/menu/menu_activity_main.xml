<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <item
        android:id="@+id/menu_activity_main_filter"
        android:icon="@drawable/ic_filter"
        android:title="@string/filter"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/filter_hour"
                android:icon="@drawable/ic_date"
                android:title="@string/filter_by_hour"
                app:showAsAction="never" />
            <item
                android:id="@+id/filter_room"
                android:icon="@drawable/ic_place"
                android:title="@string/filter_by_room"
                app:showAsAction="never" />

            <item
                android:id="@+id/filter_reset"
                android:icon="@drawable/ic_reset"
                android:title="@string/filter_reset"
                app:showAsAction="never" />
        </menu>
    </item>
</menu>